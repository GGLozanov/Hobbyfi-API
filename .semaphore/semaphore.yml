version: v1.0
name: Pre-Deployment
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Install Dependencies
    task:
      jobs:
        - name: Composer
          commands:
            - checkout
            - sem-version php 7.4
            - sudo apt-get install autoconf zlib1g-dev php-dev php-pear
            - sudo apt update && sudo apt install gcc
            - sudo pecl install grpc
            - echo "extension=grpc.so" > ~/etc/php/7.4/apache2/php.ini
            - sudo pecl install protobuf
            - echo "extension=protobuf.so" > ~/etc/php/7.4/apache2/php.ini
            - cache restore
            - composer install
            - cache store
      secrets:
        - name: dockerSecrets
        - name: projectHobbyfiAPISecrets
promotions:
  - name: Deployment
    pipeline_file: pipeline_2.yml
    auto_promote:
      when: branch = 'master' OR branch =~ '^release\/'
